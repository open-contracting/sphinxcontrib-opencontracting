name: Lint
on: [push, pull_request]
jobs:
  lint:
    uses: open-contracting/.github/.github/workflows/lint.yml@main
    permissions:
      contents: read
    with:
      python-version: '3.10'
      # Simplified schema example in docs/
      ocds-schema-exceptions: schema.json
